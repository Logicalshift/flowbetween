///
/// Contains extra settings for the layout manager
///
#[derive(Clone, Debug)]
pub struct LayoutSettings {
    /// True if the item should be laid out so that it clips to the viewport of the layout control
    pub clip_to_viewport: bool
}
